# IMPORTANT:
## BUGS
- projectiles only create an impact emitter on the target and on walls, not on other sprites
- RELOAD_ROOM event crashes in combination with emitters
- "fullscreen" property in settings.json doesn't get properly saved
- make sure that windowW and windowH are not out of bounds when not in fullscreen

## FEATURES/REFACTORS:
- Core
-- load weapons.json data into weaponData structs in Preload
-- do I need a base GameObject class? Or is that too much overhead for too little gain?
-- collision objects should be able to span multiple layers
-- auto load most of the assets instead of manually adding them to Preload.cpp
-- custom keyboard keys and gamepad buttons

- Dungeon
-- procedural room generation
-- dynamically show a shadow over a room entrance if the adjacent room is dark
-- unify Room::objectStates and TileMap::dynamicObjects? same data, two different places

- Sprites
-- drawing priority/layer system
-- Pathfinding
-- dialogue options
-- create a Sprite.fromJson() method to avoid duplications in InGame.cpp

# NOT SO IMPORTANT:
## FEATURES/REFACTORS:
- add a "persistent" property to event listeners (non-persistent listeners are removed after one event)
- Too much coupling between Game and Sprite?
- when predrawing the Tilemap chunks, don't store textures that are completely transparent
- does it make sense for Behaviors to have a draw method, or can this be solved differently?
- don't check event listeners for dispatched delayed events with callbacks
- add a "unique" property to addListener that removes old listeners if they exist
- Weapon hurtbox should appear only a fraction of the weapon's lifetime (TODO test this)
- repeated events that go infinite
- Error check for a wrong emitter texture key
- Sprite.jump() method: add argument "jumpHeight" that roughly corresponds to the number of pixels
- talking to an NPC should have the least priority when pressing the "interact" button and two or more interactibles are within reach
- don't show the "<<" and ">>" help texts in the menu if there is no next screen to that side
- add xbox button textures to the inventory/menus when using a gamepad
- check for hard-coded display texts in code and move them to texts.json
- give the Signpost some collision
- add a light cone to items that come from chests (if in a dark room) (or maybe draw them over the darkness layer?)
- make Game.getPlayer() obsolete
- give enemies certain weapon weaknesses
- the shader keys should be hardcoded in the functions in CircleOverlay.cpp
- Music/Sounds:
-- Inventory select item --> different sound
- TileMap: add support for multiple tilemaps (and respect the "firstgid" attribute)
- reconsider use of size_t in UI scenes (maybe use ints or floats instead)

## BUGS:
- RandomWalkBehavior: choose new target after some time if the current target can't be reached
- save game thumbnail is broken after saving in GameOver scene

# RELEASE CHECKLIST
- turn on sounds and music
- hand painted title image
- consistent source code formatting (brackets!)
-- should all enums be UPPERCASE?
- test xbox controller